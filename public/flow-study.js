import {exactState,averageVelocity,trainingExample} from './flow-math.js';
import {runPython,sampleCode,trainCode} from './python-lab.js';
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const ids=['flow-matching','mean-flow','improved-mean-flow'];
const names=['Flow Matching','MeanFlow','Improved MeanFlow'];
const fmt=x=>Number(x.toFixed(6)).toString();
const primers=[
 ['Vectors and latent space','A vector is an ordered list of numbers. (3,4) can mean “3 steps right, 4 steps up.” Adding vectors adds matching entries. An image is also a list of numbers; a VAE latent is a learned compact representation, not necessarily a smaller photograph. A velocity output needs one entry for each state entry.','latent'],
 ['Functions, parameters and subscripts','f(x)=2x+1 is a rule: input 3 gives output 7. In uθ(z,r,t), z,r,t are inputs; θ is the adjustable collection of weights defining the rule. A subscript t in zₜ labels the state at time t; it is not multiplication by t. Greek letters are just names.','mlp'],
 ['Linear interpolation','(1−t)x+tε is a weighted mixture. At t=0 you get x; at t=1 you get ε. With x=−1, ε=2 and t=0.6, you get 0.8. Each coordinate is mixed independently. The coefficients add to 1.','ode'],
 ['Random samples, expectation and conditioning','E means an average, weighted by probability. E[Y|Z=z] averages only cases compatible with the information Z=z. If two equally likely cases have Y=1 and Y=3, the conditional mean is 2. Gaussian noise has a bell-shaped distribution; N(0,I) means mean-zero coordinates with identity covariance.','probability'],
 ['Squared error and the norm','The norm measures the size of a vector. Squared Euclidean norm adds squared entries: ‖(3,4)‖²=9+16=25. Squaring makes positive and negative errors both costly. Averaging this error over a batch gives a training loss. Minimizing conditional squared error predicts the conditional mean.','erm'],
 ['Derivatives and partial derivatives','A derivative is the limiting slope: change in output divided by a tiny change in input. If f(t)=t², its slope at t=3 is 6. A partial derivative varies just one input: for u=az+bt, ∂zu=a and ∂tu=b. “Partial” does not mean an approximate derivative.','gradient'],
 ['Integrals and the fundamental theorem','An integral adds many tiny contributions. Velocity × a tiny duration is a tiny displacement; adding all slices gives the trip’s displacement. For velocity 2s from s=0 to 1, the integral is 1. Differentiating an integral with a moving upper limit returns the value at that limit.','ode'],
 ['Product rule and chain rule','If both factors change, d(fg)/dt=f′g+fg′. Thus d[(t−r)u]/dt=u+(t−r)du/dt when r is fixed. If u changes through its moving input z and through t, add both routes: du/dt=(∂zu)(dz/dt)+∂tu. For u=2z+3t and dz/dt=4, the total derivative is 11, not 3.','gradient'],
 ['Jacobian-vector product (JVP)','For many inputs and outputs, a Jacobian is a table of partial derivatives. Multiplying it by a direction tells how outputs change when inputs move together in that direction. For u=2z+3t+5r and direction (4,0,1) in (z,r,t) order, the JVP is 2×4+5×0+3×1=11. Autodiff can compute this without storing the whole table.','gradient'],
 ['Stop-gradient and weight updates','sg(q) has value q, but gradient 0 through that occurrence. For loss (a−sg(2a))² at a=1, the value is 1 and the gradient is −2. Removing sg gives loss a² and gradient +2. Same numerical value at that point; different update. “Stop” does not freeze all network weights.','ste'],
 ['ODEs, Euler steps and negative time','An ODE specifies a local change, dz/dt=v(z,t). Euler says new state ≈ old state + time change × velocity. Here the time change is −h, so we subtract h v. For v=z, z=2 and h=0.1, the next approximation is 1.8. A numerical step is an approximation even with an exact velocity.','ode'],
 ['Tokens, attention and normalization','A token is a vector representing one patch or one condition. Attention forms weighted mixtures of token information; softmax converts scores into positive weights that sum to 1. Normalization rescales features; adaLN changes that scaling using a condition. Context tokens instead let attention access conditions explicitly.','attention'],
 ['Matrices, transposes and attention scores','A matrix is a rectangular table of numbers. Multiplying a matrix by a vector forms weighted sums. The transpose, marked ᵀ, swaps rows and columns. In attention, each token makes a query Q, a key K and a value V using learned matrices W. Query–key dot products are sums of coordinate products: (1,2)·(3,4)=11. Dividing scores by √d controls their scale before softmax makes mixing weights.','attention'],
 ['Densities and the continuity equation','For a continuous state, p(z) is probability density, not the probability of one exact point. Integrating density over a region gives its probability. ∂ₜp measures how density changes with time; ∇·(pv), called divergence, measures local net outward probability flow. Their sum being zero says probability is transported, not created or destroyed. Think of water moving through a small box: outward flow lowers what remains inside.','probability'],
 ['Limits and a zero-length interval','A limit describes the value approached as an input gets close to a boundary. At r=t, displacement divided by duration looks like 0/0, which is undefined. We define the average velocity there by its continuous limit as the interval shrinks, giving instantaneous velocity. A neural network can accept equal times directly; it need not literally divide by zero.','ode'],
 ['NFE and FID are different measurements','NFE counts neural function evaluations during generation. FID compares means and covariances of real and generated samples in a feature space; smaller is usually better under a fixed evaluation protocol. One measures part of compute cost, the other a distributional discrepancy. Neither alone proves usefulness, originality, safety, or quality for a thesis task.','generation-metrics'],
 ['Exponents, exp and tiny differences','exp(s)=eˢ with e≈2.71828. It is its own derivative, which makes dz/dt=z easy to solve. expm1(s) computes exp(s)−1 accurately near zero; subtracting two nearly equal floating-point numbers can lose accuracy. The lab uses it for very short intervals.','ode']
];
export function flowVisualMarkup(){return `<div class="visual-card flow-visual"><div class="visual-heading"><span class="eyebrow">ONE TRAJECTORY, TWO KINDS OF VELOCITY</span><span class="live-badge">Interactive</span></div><div id="figure"></div><div id="visual-stats" class="visual-stats" aria-live="polite"></div><div class="flow-controls"><label for="flow-time">Current time t <output id="flow-time-value"></output></label><input id="flow-time" type="range" min="0" max="1" step="0.01" value="1"><label for="flow-start">Destination r (kept ≤ t) <output id="flow-start-value"></output></label><input id="flow-start" type="range" min="0" max="1" step="0.01" value="0"></div><p class="caption">Exact teaching ODE dz/dt=z, z(1)=2. Green: true trajectory. Orange: one backward Euler step using the current velocity. Blue: displacement using the exact interval average. This is an analytic illustration, not a trained generator or a paper result. At r=t the average is its limiting instantaneous value.</p></div>`;}
export function mountFlowVisual(){
 const tInput=document.querySelector('#flow-time'),rInput=document.querySelector('#flow-start');
 function draw(){const t=+tInput.value;rInput.max=t;if(+rInput.value>t)rInput.value=t;const r=+rInput.value,z=exactState(t),target=exactState(r),u=averageVelocity(z,r,t),euler=z-(t-r)*z;
 document.querySelector('#flow-time-value').value=t.toFixed(2);document.querySelector('#flow-start-value').value=r.toFixed(2);
 const X=t=>55+t*490,Y=z=>260-z*100;
 const points=Array.from({length:101},(_,i)=>`${X(i/100)},${Y(exactState(i/100))}`).join(' ');
 document.querySelector('#figure').innerHTML=`<svg viewBox="0 0 620 320" role="img" aria-label="Exact exponential trajectory, interval displacement, and Euler approximation"><title>Velocity and displacement on a known trajectory</title><desc>At t=${t}, z=${fmt(z)}. Destination at r=${r}: exact ${fmt(target)}, Euler ${fmt(euler)}.</desc><line x1="55" y1="260" x2="570" y2="260"/><line x1="55" y1="260" x2="55" y2="30"/><text x="60" y="25">state z</text><text x="370" y="303">time t (data → noise)</text><text x="50" y="283">0</text><text x="540" y="283">1</text><polyline points="${points}" fill="none" stroke="#527555" stroke-width="4"/><path d="M ${X(t)} ${Y(z)} L ${X(r)} ${Y(euler)}" stroke="#c66543" stroke-width="3" stroke-dasharray="7 5" fill="none"/><path d="M ${X(t)} ${Y(z)} L ${X(r)} ${Y(target)}" stroke="#386b86" stroke-width="3" fill="none"/><circle cx="${X(t)}" cy="${Y(z)}" r="7" fill="#223a35"/><circle cx="${X(r)}" cy="${Y(target)}" r="6" fill="#386b86"/><circle cx="${X(r)}" cy="${Y(euler)}" r="5" fill="#c66543"/></svg>`;
 document.querySelector('#visual-stats').innerHTML=[['Instantaneous v',z],['Interval average u',u],['Euler endpoint error',Math.abs(euler-target)]].map(([label,value])=>`<div><strong>${fmt(value)}</strong><span>${label}</span></div>`).join('');
 }[tInput,rInput].forEach(el=>el.addEventListener('input',draw));draw();
}
function architecture(nodes){return `<ol class="architecture">${nodes.map(([title,action,shape],i)=>`<li><span class="arch-index">${i+1}</span><div><h4>${esc(title)}</h4><p>${esc(action)}</p><small>${esc(shape)}</small></div></li>`).join('')}</ol>`;}
function referenceCode(id){const fm=id==='flow-matching';return `# Core unguided PyTorch sketch; model(z, r, t) returns a velocity.
# Times broadcast over state coordinates; omit r for an FM-only model.
# Guided targets, loss weighting, EMA and training schedules are omitted.
import torch
from torch.func import jvp

z = (1 - t) * x + t * noise
conditional = noise - x
${fm?`prediction = model(z, torch.zeros_like(r), t)
loss = (prediction - conditional).square().mean()`:`${id==='mean-flow'?'direction = conditional':'direction = model(z, t, t).detach()'}
u, dudt = jvp(model, (z, r, t),
              (direction, torch.zeros_like(r), torch.ones_like(t)))
${id==='mean-flow'?`target = (conditional - (t - r) * dudt).detach()
loss = (u - target).square().mean()`:`prediction = u + (t - r) * dudt.detach()
loss = (prediction - conditional).square().mean()`}`}
optimizer.zero_grad()
loss.backward()
optimizer.step()`;}
export function studyMarkup(c){if(!c.study)return '';const s=c.study;
 return `<div class="paper-study"><nav class="study-nav" aria-label="Flow paper sequence">${ids.map((id,i)=>`<a href="#concept/${id}" ${id===c.id?'aria-current="page"':''}><small>0${i+1}</small>${names[i]}</a>`).join('')}</nav><div class="study-intro"><p class="eyebrow">PAPER WALKTHROUGH / 0${s.number}</p><h2>${esc(s.subtitle)}</h2><p>${esc(s.change)}</p><p class="muted">Read in order, or open only the math help you need. The same notation and experiments carry across all three chapters.</p></div>
 <details class="math-help"><summary>Optional math help · start below calculus level</summary><p>You do not need all these tools at once. Expand a term when it first becomes unfamiliar.</p>${primers.map(([title,body,id])=>`<details><summary>${esc(title)}</summary><p>${esc(body)}</p><a href="#concept/${id}">Explore the related foundation →</a></details>`).join('')}</details>
 <div class="study-contents"><strong>In this chapter</strong><ol>${s.sections.map((sec,i)=>`<li><button class="section-jump" data-section="${i}">${esc(sec.title)}</button></li>`).join('')}<li><button class="section-jump" data-section="architecture">Architecture map</button></li><li><button class="section-jump" data-section="algorithm">Training microscope</button></li><li><button class="section-jump" data-section="lab">Editable code lab</button></li></ol></div>
 ${s.sections.map((sec,i)=>`<section class="study-section" id="study-${i}" tabindex="-1"><p class="eyebrow">${esc(sec.kind)}</p><h3>${esc(sec.title)}</h3>${sec.body.map(p=>`<p>${esc(p)}</p>`).join('')}${sec.formula?`<div class="study-equation" aria-label="Mathematical expression">${esc(sec.formula)}</div>`:''}</section>`).join('')}
 <section class="study-section" id="study-architecture" tabindex="-1"><p class="eyebrow">FOLLOW THE TENSORS</p><h3>Architecture map</h3><p>Follow the arrows from representation to prediction. Training-only operations are labeled explicitly.</p>${architecture(s.architecture)}</section>
 <section class="study-section" id="study-algorithm" tabindex="-1"><p class="eyebrow">TRAINING MICROSCOPE</p><h3>Inspect every operation</h3><p>One scalar example, shared across the chapters. ${c.id==='flow-matching'?'The local-velocity model is v=0.5z+0.2t+0.1. It ignores r; the destination matters only for interval methods.':'The interval model is u=0.5z+0.2t−0.1r+0.1.'}</p><label for="algorithm-time">Time t <output id="algorithm-time-value">0.60</output></label><input id="algorithm-time" type="range" min="0" max="1" step="0.01" value="0.6"><label for="algorithm-start">Destination r <output id="algorithm-start-value">0.20</output></label><input id="algorithm-start" type="range" min="0" max="0.6" step="0.01" value="0.2"><div class="algorithm-buttons" role="group" aria-label="Training steps">${['Draw & mix','Predict','Direction','Derivative','Loss','Update'].map((s,i)=>`<button data-stage="${i}" aria-pressed="${i===0}">${i+1}. ${s}</button>`).join('')}</div><div id="algorithm-panel" aria-live="polite"></div><p class="caption">A single-example arithmetic microscope, not proof of convergence or a comparison of trained model quality. At r=t both interval corrections become zero.</p></section>
 <section class="study-section code-lab" id="study-lab" tabindex="-1"><p class="eyebrow">CHANGE IT. RUN IT. INSPECT THE RESULT.</p><h3>Editable Python experiment</h3><div class="lab-modes" role="group" aria-label="Experiment"><button data-experiment="sample" aria-pressed="true">Sampling trajectory</button><button data-experiment="train" aria-pressed="false">Scalar training</button></div><p id="experiment-description"></p><label for="python-code">Experiment code</label><textarea id="python-code" rows="17" spellcheck="false" autocapitalize="off" autocomplete="off" aria-describedby="python-limits"></textarea><div class="lab-actions"><button id="run-python" class="button primary">Run code →</button><button id="reset-python" class="button">Reset example</button><button id="download-python" class="button">Download .py</button></div><p id="python-limits" class="caption">Runs locally in a small numeric Python-compatible interpreter, not full Python or PyTorch. Supports assignments, arithmetic, for/range loops, print(x,y), import math, math.exp/expm1/sqrt/sin/cos, abs, min and max. No packages, files or network access. The examples also run in standard Python. Loops and output have limits.</p><p id="python-status" role="status"></p><div id="python-chart"></div><details class="lab-output"><summary>Printed output · horizontal, vertical</summary><pre id="python-output"></pre></details><details class="reference-code"><summary>Bridge to neural-network training · PyTorch sketch</summary><p>This shows the tensor/autodiff version of the core objective. It requires PyTorch, a defined model, data and optimizer outside this browser. It is not the full paper training recipe.</p><pre><code>${esc(referenceCode(c.id))}</code></pre><a href="./examples/flow_training.py" download>Download a complete small PyTorch example →</a></details></section>
 <section class="study-section"><p class="eyebrow">COMPARE THE METHODS</p><h3>What was reused, and what changed?</h3><div class="comparison-scroll" tabindex="0" role="region" aria-label="Method comparison table"><table><thead><tr><th>Choice</th><th>Flow Matching</th><th>MeanFlow</th><th>iMF</th></tr></thead><tbody><tr><th>Core output</th><td>Local vθ</td><td>Interval uθ</td><td>Interval uθ</td></tr><tr><th>Time inputs</th><td>t</td><td>r,t</td><td>r,t</td></tr><tr><th>JVP direction</th><td>None</td><td>Pair label ε−x</td><td>Predicted vθ</td></tr><tr><th>Basic loss</th><td>vθ vs ε−x</td><td>uθ vs stopped corrected target</td><td>Compound Vθ vs ε−x</td></tr><tr><th>Sampling</th><td>Integrate local field</td><td>One or few interval jumps</td><td>Same interval jumps</td></tr><tr><th>Guidance in cited recipe</th><td>Depends on method</td><td>Fixed training scale</td><td>Scale/interval as inputs</td></tr><tr><th>Architecture</th><td>Not specified by objective</td><td>DiT-style in image study</td><td>Multiple context tokens in image study</td></tr></tbody></table></div></section>
 <section class="study-section"><p class="eyebrow">TOWARD A THESIS QUESTION</p><h3>Experiments you could build on</h3><ul>${s.research.map(p=>`<li>${esc(p)}</li>`).join('')}</ul></section><section class="study-section paper-sources"><h3>Paper trail</h3><p>Primary papers and author code support the method descriptions. The scalar examples, explanations and diagrams here are teaching constructions.</p><ul>${s.sources.map(([label,url])=>`<li><a href="${esc(url)}" target="_blank" rel="noreferrer">${esc(label)} ↗</a></li>`).join('')}</ul></section></div>`;
}
function chart(rows,mode){
 const xs=rows.map(p=>p[0]),ys=rows.map(p=>p[1]);const xmin=Math.min(...xs),xmax=Math.max(...xs),ymin=Math.min(0,...ys),ymax=Math.max(...ys);
 const X=v=>60+(v-xmin)/(xmax-xmin||1)*490,Y=v=>250-(v-ymin)/(ymax-ymin||1)*205;
 return `<svg viewBox="0 0 620 315" role="img" aria-label="${mode==='sample'?'State versus time':'Loss versus training update'} from printed code output"><title>Output of the edited program</title><desc>${rows.length} printed points. First (${rows[0].map(fmt).join(', ')}); last (${rows.at(-1).map(fmt).join(', ')}).</desc><line x1="60" y1="250" x2="560" y2="250"/><line x1="60" y1="250" x2="60" y2="35"/><text x="60" y="24">${mode==='sample'?'state z':'squared error'}</text><text x="55" y="275">${fmt(xmin)}</text><text x="510" y="275">${fmt(xmax)}</text><text x="360" y="305">${mode==='sample'?'time t (runs 1 → 0)':'training update'}</text><polyline fill="none" stroke="#386b86" stroke-width="3" points="${rows.map(([x,y])=>`${X(x)},${Y(y)}`).join(' ')}"/>${rows.filter((_,i)=>i%Math.max(1,Math.ceil(rows.length/80))===0||i===rows.length-1).map(([x,y])=>`<circle cx="${X(x)}" cy="${Y(y)}" r="3" fill="#c66543"/>`).join('')}<text x="70" y="48">max ${fmt(ymax)}</text><text x="70" y="240">min ${fmt(ymin)}</text></svg>`;
}
export function mountStudy(c){if(!c.study)return;
 document.querySelectorAll('.section-jump').forEach(b=>b.addEventListener('click',()=>{const target=document.getElementById(`study-${b.dataset.section}`);target.scrollIntoView();target.focus({preventScroll:true});}));
 let stage=0;const time=document.querySelector('#algorithm-time'),start=document.querySelector('#algorithm-start');
 function drawAlgorithm(){const t=+time.value;start.max=t;if(+start.value>t)start.value=t;const r=+start.value;start.disabled=c.id==='flow-matching';document.querySelector('#algorithm-time-value').value=t.toFixed(2);document.querySelector('#algorithm-start-value').value=r.toFixed(2);const q=trainingExample(c.id,{t,r}),fm=c.id==='flow-matching';
 const steps=[
 ['Draw endpoints; construct the input',['x = −1','ε = 2',`t = ${fmt(t)}`],`z = (1−t)x+tε = ${fmt(q.z)}`,'Only a simple mixture is needed; no model-generated training trajectory. The available pair velocity is ε−x=3.'],
 ['Read the network output',[`z = ${fmt(q.z)}`,...(fm?[]:[`r = ${fmt(r)}`]),`t = ${fmt(t)}`],`${fm?'vθ':'uθ'} = ${fmt(q.u)}`,'The affine network is a transparent stand-in for a large neural network. It outputs a velocity-shaped quantity.'],
 [fm?'No derivative direction is needed':'Choose the derivative direction',[c.id==='improved-mean-flow'?'boundary: uθ(z,t,t)':'pair label: ε−x'],fm?'FM skips the JVP':`direction = ${fmt(q.tangent)}`,fm?'Ordinary velocity regression has no interval derivative correction.':c.id==='improved-mean-flow'?'This is the decisive iMF change. The direction is predicted from the state, not taken from its sampled endpoint pair.':'Original MeanFlow uses the sampled conditional velocity here as a practical substitute for the marginal direction.'],
 [fm?'No derivative correction':'Calculate the JVP',fm?['direct prediction']:[`∂zu = 0.5`,`direction = ${fmt(q.tangent)}`,'∂tu = 0.2'],fm?'correction = 0':`D = ${fmt(q.derivative)}; hD = ${fmt(q.correction)}`,fm?'Proceed directly to the velocity loss.':'Multiply the state derivative by the chosen direction, add the direct time derivative, then multiply by t−r.'],
 ['Compute squared error',[`prediction = ${fmt(q.u)}`,`correction = ${fmt(q.correction)}`,'pair target = 3'],`loss = (${fmt(q.error)})² = ${fmt(q.loss)}`,c.id==='mean-flow'?`Equivalently compare u=${fmt(q.u)} to the stopped target ${fmt(q.target)}. The target is recomputed after updates.`:'Compare the direct or compound velocity prediction to the sampled velocity target. A single loss value cannot rank trained methods.'],
 ['Update the weights',['learning rate η = 0.03',`∂loss/∂d = 2 error = ${fmt(2*q.error)}`],`d: 0.1 → ${fmt(.1-.03*2*q.error)}`,`This shows the bias update only. ${fm?'':'Treat the derivative correction as fixed during this gradient calculation. '}The code lab updates all four coefficients, then recomputes everything on the next iteration.`]
 ];const [title,inputs,result,why]=steps[stage];
 document.querySelector('#algorithm-panel').innerHTML=`<h4>${stage+1}. ${esc(title)}</h4><div class="operation-graph"><div>${inputs.map(x=>`<span>${esc(x)}</span>`).join('')}</div><span class="operation-arrow" aria-hidden="true">↓</span><strong>${esc(result)}</strong></div><p>${esc(why)}</p>`;
 document.querySelectorAll('[data-stage]').forEach(b=>b.setAttribute('aria-pressed',+b.dataset.stage===stage));
 }
 document.querySelectorAll('[data-stage]').forEach(b=>b.addEventListener('click',()=>{stage=+b.dataset.stage;drawAlgorithm();}));[time,start].forEach(el=>el.addEventListener('input',drawAlgorithm));drawAlgorithm();
 let mode='sample';const editor=document.querySelector('#python-code'),status=document.querySelector('#python-status');
 function run(){try{const result=runPython(editor.value);document.querySelector('#python-chart').innerHTML=chart(result.rows,mode);document.querySelector('#python-output').textContent=result.rows.map(row=>row.map(fmt).join('  ')).join('\n');status.textContent=`${result.rows.length} points rendered. Final output: ${result.rows.at(-1).map(fmt).join(', ')}.`;status.className='lab-success';}catch(e){status.textContent=e.message;status.className='lab-error';document.querySelector('#python-chart').replaceChildren();document.querySelector('#python-output').textContent='';}}
 function reset(){editor.value=mode==='sample'?sampleCode(c.id):trainCode(c.id);document.querySelector('#experiment-description').textContent=mode==='sample'?'Edit steps or the starting state. The FM example uses Euler on an exact field; MF and iMF use its exact interval average as an oracle. The same MF/iMF sampler is intentional. These curves isolate integration, not learned-model error.':'Edit the learning rate, time gap or initial coefficients. This fits one fixed scalar pair using manually derived gradients. It demonstrates update mechanics, not population learning or comparative image quality. print(update, squared_error) produces the graph.';run();}
 document.querySelectorAll('[data-experiment]').forEach(b=>b.addEventListener('click',()=>{mode=b.dataset.experiment;document.querySelectorAll('[data-experiment]').forEach(x=>x.setAttribute('aria-pressed',x===b));reset();}));
 editor.addEventListener('input',()=>{status.textContent='Code changed. Run it to refresh the output.';status.className='';});
 document.querySelector('#run-python').addEventListener('click',run);document.querySelector('#reset-python').addEventListener('click',reset);
 document.querySelector('#download-python').addEventListener('click',()=>{const url=URL.createObjectURL(new Blob([editor.value+'\n'],{type:'text/x-python'}));const a=document.createElement('a');a.href=url;a.download=`${c.id}-${mode}.py`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});reset();
}
